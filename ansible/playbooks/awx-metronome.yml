---
- name: AWX
  hosts: localhost
  connection: local
  gather_facts: false
  become: false

  tasks:

    - name: Include awx role (metronome)
      include_role:
        name: awx
        tasks_from: metronome.yml
